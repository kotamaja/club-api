import { Component } from '@angular/core';

/**
 * Placeholder page displayed while the real implementation is pending.
 */
@Component({
  selector: 'app-select-organization-page',
  template: '<p>No Select Organization page</p>',
})
export class SelectOrganizationPageComponent {
}
